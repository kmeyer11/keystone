CLAUDE_CODE_OAUTH_TOKEN=

# Optional, later. Only needed if you actually want Higgsfield's AI video
# generation — it costs money (paid plans start ~$19/mo, free tier is a
# trivial daily trickle of credits). The "real clips cut to music" version of
# the highlight-edit feature (see CLAUDE.md) doesn't need this at all — that's
# a free ffmpeg job. Leave blank until you've decided you want it.
HF_API_KEY_ID=
HF_API_KEY_SECRET=
